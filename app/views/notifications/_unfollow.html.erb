<% @notifications.each do |notification| %>
  <li class="row border-bottom mb-2 ml-auto py-2">
    <div class="col-md-10">
      <p><span class="user">ユーザー名:<%= link_to current_user.name, users_path(current_user) %></span></p>
      <p><span class="content"><%= @followed_user.name %>からフォローを外されました...</span></p>
      <p><span class="timestamp">
        フォローを外されたのは<%= time_ago_in_words(relationships.created_at) %>前です。
      </span></p>
    </div>
  </li>
<% end %>
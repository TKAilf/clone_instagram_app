<% provide(:title, "全ユーザー") %>
<%= render "layouts/header" %>

<h1>全ユーザー</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
</ul>

<%= will_paginate %>
<% if object.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger" role="alert">
      このフォームには<%= pluralize(object.errors.count, "error") %>が含まれています。
    </div>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
      <li> <%= msg %>  </li>
      <% end %>
    </ul>
  </div>
<% end %>
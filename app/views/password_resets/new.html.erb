<% provide(:title, "パスワードリセット") %>
<%= render 'layouts/header' if !current_user.nil? %>
<h1>パスワードリセット画面</h1>
<div class="row justify-content-center">
  <div class="p-5 col-md-7">
    <p>下記のメールアドレスにパスワード変更用のメールを送信します。</p>
    <%= form_with scope: :password_reset, url: password_resets_path, local: true do |f| %>
      <div class="my-4 form-group">
        <%= f.label :email, "メールアドレス" %>
        <%= f.email_field :email, class: "form-control", id: "email", type: "email", aria: { describedby: "emailHelp"}, placeholder: "メールアドレス" %>
      </div>
      <div class="center">
        <%= f.submit "送信", class: "px-5 mb-3 btn btn-primary" %>
      </div>
    <% end %>
  </div>
</div>